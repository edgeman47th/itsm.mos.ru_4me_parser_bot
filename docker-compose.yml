version: '3'

services:
  itsm-4me-bot:
    build: .
    container_name: itsm.mos.ru_4me_parser_bot
    restart: unless-stopped
    network_mode: "host"
    env_file:
      - .env
    volumes:
      - ./db:/app/db
      - ./last_check.json:/app/last_check.json